# Задание 5 по Модулю 5. Написать модуль ядра для своей версии ядра, который будет обмениваться информацией с userspace через netlink.  
Первая сборка прошла успешно, но с предупреждениями которые касаются файла "user.c". Файл "netlink.c" был собран без ошибок и предупреждений. Модуль и пользовательская программа успешно сработали (Скриншот 1).  
Файле "user.c", в строке "38:nlh = (struct nlmsghdr *)malloc(NLMSG_SPACE(MAX_PAYLOAD));" убрал явное объявление типа указателя.  
Добавил библотеку #include <unistd.h> чтобы корректно использовать функцию getpid() и close() (До этого были предупреждения при компиляции).  
В файле модуля ядра глобальную структуру сделал статической (Чтобы ограничить области видимости только исполняемым файлом).  
